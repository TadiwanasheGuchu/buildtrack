# Frontend environment variables
# Copy this file to .env and fill in your values

# API URL — FastAPI backend
VITE_API_URL=http://localhost:8000

# Set to true to use mock auth (no backend required, for UI development)
VITE_DEMO_MODE=true
